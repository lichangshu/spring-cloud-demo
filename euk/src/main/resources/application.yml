###
#
###
server:
  port: 8080

#
spring:
  application:
    name: EUK
    
#
eureka:
  instance:
    hostname: def
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

#

